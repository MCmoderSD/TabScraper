{
  "name": "tab-scraper",
  "version": "1.1.0",
  "description": "",
  "main": "dist/background.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist/*.js.map"
  },

  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "typescript": "^5.9.3"
  }
}